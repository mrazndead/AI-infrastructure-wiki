// Service disabled to remove external dependencies
export const getGeminiClient = () => {
  return null;
};

export const generateInfrastructureAdvice = async () => {
  return "AI Chat is currently disabled.";
};